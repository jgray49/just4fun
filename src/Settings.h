#pragma once
namespace Settings {
	// menu
	static bool bShowMenu = true;

	// drawings
	static bool bDrawAtkRange = true;
	static bool bDrawQRange = true;
	static bool bDrawWRange = true;
	static bool bDrawERange = true;
	static bool bDrawRRange = true;


	

	// orbwalker
	static bool bOrbwalkEnabled = true;

	// evade

	// scripts
}